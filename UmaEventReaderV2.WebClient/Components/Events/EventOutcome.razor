@using UmaEventReaderV2.Models.Entities

<MudChip T="UmaEventChoiceOutcome" Class="@Outcome.Type.ToString().ToLower()">
    @Outcome
</MudChip>

@code {
    [EditorRequired]
    [Parameter]
    public required UmaEventChoiceOutcome Outcome { get; set; }
}