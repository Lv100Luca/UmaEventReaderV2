<MudChip T="UmaEventChoiceOutcome" Class="@Outcome.Type.ToString().ToLower()">
    @Outcome
</MudChip>

@code {
    [EditorRequired]
    [Parameter]
    public required EventChoiceOutcome Outcome { get; set; }
}